// Todo Tasks 101:
// title
// priority
// dueDate
// task
// note
// tags

const Task = (title, priority = 0, dueDate, task, note, tags) => {
    return {title, priority, dueDate, task, note, tags}
}